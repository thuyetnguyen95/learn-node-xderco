extends ../layouts/main.pug

block content

  h2 Detail user
  hr
  p ID: #{user.id}
  p Name: #{user.name}
  p Phone number: #{user.phoneNumber}
  hr
  a(href="/users" class="btn btn-outline-secondary") Back